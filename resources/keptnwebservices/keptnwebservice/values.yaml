# Default values for helm-guestbook.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image: docker.io/grabnerandi/keptnwebservice:2.0.0
pullPolicy: Always

service:
  name: keptnwebservice
  type: ClusterIP
  port: 8080
  targetPort: 8080